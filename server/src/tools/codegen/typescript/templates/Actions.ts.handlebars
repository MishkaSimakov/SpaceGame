{{! This file is a template for Typescript actions description file. }}
/*
This file is autogenerated using server/src/tools/codegen/Codegen.ts tool.
Do not change it by hand! Instead, rerun generation script.
*/

import {constructAction} from "./ActionsHelpers";
import * as Types from "./Types";

{{#each actions}}

/**
{{#if description}}
 * {{{ formatForDocstring description }}}
{{/if}}
{{#each payload }}
 * @param  {{ name }}  {{{ formatForDocstring description }}}
{{/each}}
{{#if meta.length }}
 *
 * Мета-параметры события:
{{#each meta }}
* @param  {{ name }}  {{{ formatForDocstring description }}}
{{/each}}
{{/if}}
 */
export function {{ name }}({{ renderArguments payload meta }}) {
    return constructAction(
        '{{ name }}',
        {
            {{~ renderPayload payload ~}}
        },
        {{#if meta.length }}
        {
            {{~ renderPayload meta ~}}
        }
        {{/if}}
    );
}

{{/each}}